# Local Application Imports
from manager import Manager

# Only run code inside if this file is run as main script, NOT an import
if __name__ == "__main__":
    manager = Manager()
    manager.root.mainloop()
